google-genai
requests
# Pinned: moviepy 2.x removed the moviepy.editor module and changed the whole
# effects/method API, which composer.py/audio.py are not written for. 1.0.3 is
# the last version with moviepy.editor. It also requires Pillow < 10 (newer
# Pillow removed Image.ANTIALIAS that moviepy 1.0.3's resize code depends on).
moviepy==1.0.3
Pillow==9.5.0
numpy==1.26.4
gTTS
edge-tts
google-api-python-client
google-auth-oauthlib
google-auth-httplib2
